<nav-bar title="{{ navTitle }}"></nav-bar>


<view class="container">
  <top-line name="我的球队" more="{{ false }}"></top-line>
  <view wx:if="{{ myTeams }}">
    <swiper class="team-swiper" indicator-dots="{{ true }}">
      <swiper-item wx:for="{{ myTeams }}" wx:key="index">
        <team-card team="{{ item }}">
          <navigator url="/pages/sub/details/team/index?id={{ item.id }}" open-type="navigate">
            <view class="active">
              <text>详情</text>
              <image class="arrow" src="{{ arrow }}" mode="aspectFill" />
            </view>
          </navigator>
        </team-card>
      </swiper-item>
    </swiper>
  </view>
  <view wx:else>
    <view class="tip">
      您还没有加入球队，请选择球队加入
    </view>
  </view>


  <view class="list">
    <top-line name="球队列表" more="<创建球队>" bind:handleButtomClick="ToTeamCreate"></top-line>
    <view wx:if="{{notMyTeams}}">
      <enlist-card wx:for="{{notMyTeams}}" wx:key="index" team="{{item}}"></enlist-card>
    </view>
    <view wx:else class="tip">
      可加入的球队列表为空，您可以点击创建自己的球队
    </view>
  </view>
</view>
<!-- 占位 -->
<view style="height: 188rpx;"></view>