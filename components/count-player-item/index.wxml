<view class="container">
  <view class="item {{ isRedTeam ? 'red-item' : 'blue-item' }}">
    <view class="avatar {{ isRedTeam ? 'red' : 'blue' }}">{{ number }}</view>
    <view class="active">
      <slot></slot>
    </view>
  </view>
</view>